// Copy to secrets.h (gitignored). Wi‑Fi is provisioned via captive portal; no SSID/password here.
#pragma once

// Public HTTPS origin only — no path, no trailing slash (e.g. https://api.example.com)
#define API_BASE_URL "https://your-api.onrender.com"

// Optional overrides (defaults shown in main.cpp if omitted):
// #define API_EVENTS_PATH "/api/v1/events"
// #define API_REGISTER_PATH "/api/gateway/register"
// #define PROVISIONING_AP_PREFIX "Helix-Gateway-"
// #define WIFI_HOSTNAME "helix-gateway"
