// Copy to secrets.h and fill in (secrets.h is gitignored if you add it).
#pragma once
#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"
#define API_BASE_URL "http://192.168.1.100:8000"  // no trailing slash
#define GATEWAY_ID "your-gateway-uuid-or-id"
