import { SystemAppLayout } from "@/components/system/SystemAppLayout";

export default function SystemLayout({ children }: { children: React.ReactNode }) {
  return <SystemAppLayout>{children}</SystemAppLayout>;
}
