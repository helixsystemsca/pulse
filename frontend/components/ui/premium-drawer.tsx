/**
 * Premium right / center panel — implemented by {@link PulseDrawer} with motion, blur, and shell dimming.
 * Import `PulseDrawer` for the canonical drawer; this alias documents the design-system name.
 */
export { PulseDrawer as PremiumDrawer } from "@/components/schedule/PulseDrawer";
