/**
 * Literal Tailwind classes for overlays portaled to `document.body` (JIT must see full strings).
 * Above {@link AppNavbar} (z-50), impersonation strip (55), schedule drag scrim (115), trash (145).
 */
export const APP_MODAL_PORTAL_Z_BASE = "z-[170]";
export const APP_MODAL_PORTAL_Z_ELEVATED = "z-[180]";
