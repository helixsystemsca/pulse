/** Barrel re-exports for the authenticated app chrome (most imports use `@/components/app/...` directly). */
export { AppLayout } from "./AppLayout";
export { AppMain } from "./AppMain";
export { AppNavbar } from "./AppNavbar";
export { AppSideNav } from "./AppSideNav";
