/** Capability helpers (re-export thin layer for `utils/` discoverability). */
export {
  hasCapability,
  resolveCapabilitiesFromSession,
  sessionHasCapability,
  PLATFORM_CAPABILITIES,
} from "@/config/platform/permissions";
