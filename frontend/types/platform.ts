/** Re-exports for consumers that prefer `types/platform` imports. */
export type {
  Department,
  DepartmentId,
  PlatformIconKey,
  PlatformModule,
  PlatformNavItem,
} from "@/config/platform/types";
export type { PlatformCapability } from "@/config/platform/permissions";
