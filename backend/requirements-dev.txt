# Local / CI automated tests (API + database)
# - Integration: PostgreSQL only (no SQLite). Leave TEST_DATABASE_URL unset to auto-start
#   postgres:16-alpine via Docker; or set TEST_DATABASE_URL to an existing DB (migrations run each session).
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
